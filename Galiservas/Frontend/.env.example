# Base URL of the shared Galileo Auth JSON API. Do not include a trailing slash.
# En el kit USB, Vite redirige /api a XAMPP (ver vite.galiservas.config.mjs).
VITE_API_URL=/api

# URL de la app Galisencia (se usa para el botón "Volver a Galisencia").
VITE_GALISENCIA_URL=http://localhost:3000
